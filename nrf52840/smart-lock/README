# nRF52840 — Smart Lock

Zephyr BLE smart lock controller. Advertises lock state and battery SOC over BLE. Implements hardware-bound identity persistence for secure BLE pairing.

## Hardware

- **MCU:** nRF52840 (Pro Micro form factor)
- **Board:** `promicro_nrf52840`
- **Interface:** Lock actuator output, BLE advertising

## Source Files

| File | Description |
|:-----|:------------|
| `src/main.c` | BLE advertising, lock state management |
| `src/battery.c` | Battery SOC measurement |
| `src/flash_log.c` | Flash-backed event logging |
| `src/fatal_hook.c` | Fatal error handler |

## Environment Setup

```bash
source ~/zephyrproject/.venv/bin/activate
export ZEPHYR_BASE=~/zephyrproject/zephyr
```

## Build

```bash
west build -p always -b promicro_nrf52840 \
  -d ~/embedded-iot-hub/nrf52840/smart-lock/build \
  ~/embedded-iot-hub/nrf52840/smart-lock \
  -- -DCONFIG_DEBUG=y
```

## Flash

```bash
sudo mkdir -p /mnt/nrf52
sudo mount /dev/sda /mnt/nrf52
sudo cp build/zephyr/zephyr.uf2 /mnt/nrf52/
sync && sudo umount /mnt/nrf52
```

## Monitor

```bash
minicom -D /dev/ttyACM0 -b 115200
```

## Teardown

```bash
deactivate
```
